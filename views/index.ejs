<%- include("partials/header.ejs") %>
<div class="post-section">
  <form class="post-form" action="/" method="post">
    <textarea
      class="post-text"
      type="text"
      placeholder="What's on your mind?"
      name="text"
    ></textarea>
    <input class="post-btn" type="submit" value="Post" />
  </form>
</div>
  <form action="/blog" method="post">
    <% if(locals.arr && locals.arr.length>0){%> <% arr.map((item, index)=>{%>
  <div class="posts">
    <p id="item"><%=item%></p>
    <button type="submit" name="id" value=<%=index%>>Edit</button>
    <input class="post-btn" type="submit" formaction="/delete" value="Delete" name="delete" />
  </div>
    <%})}else{%>
    <p class="default-text m-3 text-center">Add new posts 📫</p>
    <%} %>
  </form>

<%- include("partials/footer.ejs") %>
